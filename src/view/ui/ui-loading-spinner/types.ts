export interface Props {
  /** Размер индикатора */
  size?: 'small' | 'medium' | 'large';

  /** Показывать текст загрузки */
  showText?: boolean;

  /** Ключ перевода для текста */
  textKey?: string;
}
